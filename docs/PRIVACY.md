# Privacy

Images are processed by the local application. The project does not intentionally upload images or OCR results to a cloud service.

EasyOCR model files may be downloaded during initial setup. Package installation also contacts Python package repositories. Users deploying on shared networks are responsible for access control, retention, reverse-proxy configuration, and local regulations.
